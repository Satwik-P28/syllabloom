# Contributing to Syllabloom

1. Search existing issues, then open one for substantial reader or import changes.
2. Fork and create a focused branch.
3. Run `npm ci`, then `npm run dev`.
4. Cover reading helpers in tests.
5. Run `npm run check` and `npm audit`.

By contributing, you agree that your contribution is licensed under MIT.
